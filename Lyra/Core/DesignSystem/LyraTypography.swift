import SwiftUI

enum LyraTypography {
    static let issueID = Font.system(.subheadline, design: .monospaced, weight: .medium)
    static let issueTitle = Font.system(.body, weight: .regular)
    static let sectionHeader = Font.system(.caption, weight: .semibold)
    static let projectTitle = Font.system(.title2, weight: .semibold)
    static let metadataLabel = Font.system(.caption, weight: .medium)
}
